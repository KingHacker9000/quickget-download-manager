# Platform Support

QDM `v0.1.0` is Windows-first.

- Windows: primary target and actively tested.
- macOS: build artifacts may exist, but are currently untested and experimental.
- Linux: build artifacts may exist, but are currently untested and experimental.

QDM does not currently claim full cross-platform support.
